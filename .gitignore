# ===== Build output (Netlify rebuilds these) =====
dist/
node_modules/
package-lock.json

# ===== KEEP THESE OFF GITHUB — stay on your computer only =====
# Some of these contain PRIVATE API keys (Stripe live key, GoHighLevel key,
# Supabase service key). They are NOT needed to run the website.
n8n/
pdfs/
emails/
assets/
hof-chat-workflow.json
*.md
*.pdf

# Local build tooling for the book reader (content is inlined into pages/book.html)
data/
generate_book.py

# ===== n8n workflows + credential files — CONTAIN LIVE TOKENS, never commit =====
*SPS*.json
*SES*.json
Facebook HOF Business Agent*.json
OpenRouter API.txt
*.txt

# OS / editor cruft
.DS_Store
Thumbs.db
.vscode/
hof-veo-clips/
